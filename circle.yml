test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/target/surefire-reports/junitreports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
general:
  artifacts:
    - "Summative/target/surefire-reports"
    - "Summative/download"

test:
  override:
    - cd "Summative"
    - cd "summative"
    - mvn test